# Next.js public — must be NEXT_PUBLIC_ prefixed to ship to the browser
NEXT_PUBLIC_API_BASE=http://localhost:5081

# Supabase (anon key only — service key stays on the .NET side)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
